import * as React from 'react';

export const PDFNotice = () => (
  <div className="pdf-notice">
    <strong>Warning:</strong> The PDF export feature is currently offline, it will be added back soon!
  </div>
);
